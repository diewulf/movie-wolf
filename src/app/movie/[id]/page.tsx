import { VideoContainer } from './container';

const MoviePage = () => {
  return (
    <div className="flex flex-col min-h-screen">
      <VideoContainer movieId={'3'} />
    </div>
  );
};
export default MoviePage;
